# 2026-02-18 — UnGouge.ai Launch Day (continued)

## Major Accomplishments

### AI-Powered Analyzer (SHIPPED)
- Built `analyzer_ai.py` — Gemini 2.5 Pro with Google Search Grounding
- Primary engine makes ~11 real-time Google searches per analysis
- Cites 15+ sources for pricing data (stripped from customer output for legal safety)
- Fallback chain: Gemini 2.5 Pro → Gemini 2.0 Flash → Static V2
- Cost: ~$0.026/analysis, ~70 seconds
- **Decision: strip source citations from reports** — Jason agreed, legal liability concern

### Bugs Fixed
- **bcrypt version break** — loosened dependency pins pulled bcrypt 4.2+ which is strict about 72-byte passwords. Pinned back to 4.0.1
- **Promo code duplicate key** — `stripe_payment_intent_id` was `promo_LAUNCH2026` (not unique). Now includes quote_id
- **Parser eating totals** — prompt told AI to include subtotals as line items. Fixed prompt + regex safety net
- **Parser $0 items** — prices embedded in descriptions ("Painting - $3,800") missed. Fixed parser prompt + post-processing extraction
- **Dashboard stats not updating** — savings calc only checked total-level diff, not individual line item overpayments. Fixed to sum per-item overpayments from analysis JSON
- **Contact Support 404** — `/support` page didn't exist, dashboard linked to it. Built full support page
- **Gateway crash (qwen model)** — `qwen2.5:32b-instruct-q4_K_M` in fallback chain but not installed. Removed from openclaw.json

### Frontend Overhaul
- **Report page**: filter/sort by assessment, quick-jump pills for gouging items, formatted assessment (paragraphs/bullets/numbered lists), vivid gauge colors
- **PDF generation**: switched from server-side ReportLab to client-side html2canvas + jsPDF (matches web report exactly)
- **Server PDF also overhauled**: structured assessment, full explanations (no truncation), row highlighting (red for gouging, orange for high, blue for suspiciously_low), page numbers, title-cased project types, clean currency
- **Promo code**: moved from Step 3 to Step 2 with Apply/Remove buttons
- **$0 warning**: yellow banner on Step 2 listing items with $0 prices
- **Savings calculation**: sums individual item overpayments, not just total diff. "Negotiation Savings: $4,200" instead of "$0"
- **"Gouging" → "Possible Gouge"** everywhere (liability protection)
- **"My Quotes" link** added to report bottom actions
- **Main header** now shows on all pages including dashboard
- **Support page** built: Scout AI chat + human@ungouge.ai + 16 FAQs in 4 categories
- **ChatWidget rebranded as "Scout"**

### Deploy Summary
- Backend revisions: 00037 through 00044 (8 deploys)
- Frontend deploys: ~8 Vercel deploys
- All committed and pushed to GitHub

## Key Decisions
- **"Possible Gouge" not "Gouging"** — softer language for liability
- **Client-side PDF** over server-side — matches web report exactly
- **Scout** as the AI assistant name (not "chatbot" or "support bot")
- **human@ungouge.ai** as the human contact — "we're real people" messaging
- **Phase 2 distribution captured**: mobile app, Custom GPT, SEO, social proof, partnerships

## Current State
- Backend: revision 00044, image `v14-dashboard-fix`
- Frontend: latest Vercel deploy
- AI analyzer: live, Gemini 2.5 Pro + Search Grounding
- Promo codes: LAUNCH2026, BETATESTER (working)
- All core E2E flows working: register → upload → parse → review → promo/pay → report → PDF

## Still TODO
- Test Stripe payment flow (without promo code)
- Test email delivery (password reset, receipts)
- Shred ~/Desktop/keys.odt
- Mobile-responsive audit
- PDF quality — html2canvas approach needs testing, may need tweaks
- Report re-analysis button (reduce refunds)
