# Executive Dashboard Security & Code Fixes — 2026-02-09

All fixes applied by Ish (subagent) on Feb 9, 2026.

---

## CRIT-05: Fix CORS ✅
- **File:** `backend/main.py`
- Changed `allow_origins=["*"]` → `allow_origins=["https://dashboard.ungouge.ai"]`
- Prevents cross-origin requests from unauthorized domains

## HIGH-01: Rate Limiting Note ✅
- **File:** `backend/main.py` — Added TODO comment near `require_auth` noting that rate limiting via `slowapi` is needed on `/auth/login` and `/auth/callback`
- **File:** `backend/requirements.txt` — Added `slowapi` dependency

## HIGH-03: XSS Protection ✅
- **Files:** `dashboard-v2.html`, `tasks.html`, `expenses.html`, `projects-ungouge.html`, `projects-youtube.html`, `project-detail.html`
- Added `escapeHtml()` function to each file
- Wrapped all user-data template literals:
  - `task.title`, `task.description`, `task.status`, `task.priority`
  - `project.name`, `project.description`
  - `expense.description`, `expense.vendor`, `expense.category`
  - Search result titles/subtitles
  - YouTube channel title

## HIGH-06: Protect Debug Endpoint ✅
- **File:** `backend/main.py`
- Added `user_info: dict = Depends(require_auth)` to `/api/debug/static`
- Endpoint now requires authentication

## HIGH-07: Fix Duplicate urlParams ✅
- **File:** `backend/static/login.html`
- Removed duplicate `const urlParams = new URLSearchParams(...)` declaration
- Now uses single declaration at top of script, reused for error checking

## HIGH-09: Fix Session Token in URL ✅
- **File:** `backend/main.py`
- Modified `auth_callback()` to set session cookie directly on the redirect response
- Changed from `RedirectResponse(url=f"/?auth_token={session_token}")` to `RedirectResponse(url="/")` with `.set_cookie()`
- Root handler's `auth_token` parameter kept as fallback but no longer the primary auth path

## LOW-02: Add Favicon ✅
- **Files:** `dashboard-v2.html`, `login.html`
- Added SVG data URI favicon (`🚀` emoji) in `<head>` to prevent 404s

## LOW-03: Remove Cookie Logging ✅
- **File:** `backend/main.py`
- Removed `print(f"🔍 All cookies: {request.cookies}")` 
- Removed `print(f"🔍 Session token: {session_token[:20] if session_token else 'None'}...")`
- Prevents leaking sensitive session data to logs

## LOW-08: Remove Build Comment ✅
- **File:** `backend/main.py`
- Removed trailing `# rebuild Mon Feb ...` comment

## MED-05: Automated Session Cleanup ✅
- **File:** `backend/main.py`
- Added `cleanup_expired_sessions()` call in `startup_event()`
- Added periodic cleanup in `require_auth()` (at most once per hour via `_last_cleanup_time` tracking)

## MED-08: Fix Category Filtering ✅
- **File:** `dashboard-v2.html`
- Replaced hardcoded `categoryProjectNames` (matched by project name) with `categoryMap` (matched by `project.category` field)
- `'ungouge'` → matches categories: `['product', 'operations', 'ungouge']`
- `'youtube'` → matches categories: `['content', 'youtube']`

## MED-10: Auth Checks on Static File Routes ✅
- **File:** `backend/main.py`
- Added `_require_session_or_redirect()` helper function
- All protected HTML routes now check `session_token` cookie and redirect to `/login.html` if invalid
- Protected routes: `tasks.html`, `expenses.html`, `project-detail.html`, `settings.html`, `projects.html`, `projects-ungouge.html`, `projects-youtube.html`
- `login.html` route left unprotected (must be accessible)

---

## Summary

| Priority | Issue | Status |
|----------|-------|--------|
| CRIT-05 | CORS wildcard | ✅ Fixed |
| HIGH-01 | Rate limiting | ✅ Noted + dep added |
| HIGH-03 | XSS protection | ✅ Fixed (6 files) |
| HIGH-06 | Debug endpoint exposed | ✅ Protected |
| HIGH-07 | Duplicate urlParams | ✅ Fixed |
| HIGH-09 | Session token in URL | ✅ Fixed |
| LOW-02 | Missing favicon | ✅ Added |
| LOW-03 | Cookie logging | ✅ Removed |
| LOW-08 | Build comment | ✅ Removed |
| MED-05 | Session cleanup | ✅ Added |
| MED-08 | Category filtering | ✅ Fixed |
| MED-10 | Static route auth | ✅ Added |

**Total: 12 fixes applied across 8 files.**
