# Alembic Configuration — UnGouge.ai
#
# NOTE: sqlalchemy.url is set dynamically in alembic/env.py from the
# DATABASE_URL environment variable.  The placeholder below is only
# used if the env var is missing (local dev default).

[alembic]
script_location = alembic
prepend_sys_path = .
version_path_separator = os

# template used to generate migration file names
# file_template = %%(rev)s_%%(slug)s
file_template = %%(year)d%%(month).2d%%(day).2d_%%(rev)s_%%(slug)s

# Logging configuration
[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
